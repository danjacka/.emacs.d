;; Fennel is a programming language that brings together the simplicity, speed,
;; and reach of Lua with the flexibility of a lisp syntax and macro system.

(use-package fennel-mode)

(provide 'setup-fennel-mode)
